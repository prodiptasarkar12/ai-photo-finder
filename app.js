const API_BASE=(window.LENSFIND_API_BASE||'').replace(/\/$/,'');
const DB_NAME='lensfind-local-test';const DB_VERSION=1;const events=[];let currentEventId=null;let eventCounter=1;let faceApiReady=null;let currentStream=null;
const dbOpen=()=>new Promise((resolve,reject)=>{const r=indexedDB.open(DB_NAME,DB_VERSION);r.onupgradeneeded=()=>{const db=r.result;if(!db.objectStoreNames.contains('events'))db.createObjectStore('events',{keyPath:'id'});if(!db.objectStoreNames.contains('photos')){const s=db.createObjectStore('photos',{keyPath:'id'});s.createIndex('eventId','eventId',{unique:false})}};r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)});
async function dbPut(store,value){const db=await dbOpen();return new Promise((resolve,reject)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put(value);tx.oncomplete=resolve;tx.onerror=()=>reject(tx.error)})}
async function dbGetAll(store){const db=await dbOpen();return new Promise((resolve,reject)=>{const r=db.transaction(store,'readonly').objectStore(store).getAll();r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
async function dbGetPhotos(eventId){const db=await dbOpen();return new Promise((resolve,reject)=>{const r=db.transaction('photos','readonly').objectStore('photos').index('eventId').getAll(eventId);r.onsuccess=()=>resolve(r.result);r.onerror=()=>reject(r.error)})}
function eventCard(e){return `<article class="event-card"><div class="event-image one"><span class="event-status">● ${e.status||'READY'}</span></div><div class="event-body"><h3>${escapeHtml(e.name)}</h3><p class="muted">${escapeHtml(e.date||'—')}</p><div class="event-meta"><span>📷 ${e.photos||0} photos</span><span>Local upload</span></div><div class="event-footer"><button class="secondary-btn" onclick="openQr('${e.id}')">QR code</button><button class="primary-btn" onclick="openGuest('${e.id}')">Open event</button></div></div></article>`}
function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]))}
function renderEvents(){document.getElementById('eventGrid').innerHTML=events.map(eventCard).join('')||'<p class="muted">No events yet. Create your first event.</p>';document.getElementById('allEvents').innerHTML=events.map(eventCard).join('')||'<p class="muted">No events yet.</p>'}
function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById(id)?.classList.add('active');window.scrollTo({top:0,behavior:'smooth'});document.querySelector('.topnav')?.classList.remove('mobile-open')}
async function openGuest(id=''){currentEventId=id||currentEventId;showPage('guest');if(currentEventId){const ps=await dbGetPhotos(currentEventId);document.getElementById('aiStatus').textContent=ps.length?`${ps.length} local photos ready for AI search.`:'No photos in this event yet.'}}
async function openQr(id=''){currentEventId=id||currentEventId;showPage('qr');const e=events.find(x=>x.id===currentEventId);const link=e?`${location.origin}${location.pathname}?event=${encodeURIComponent(e.id)}&view=guest`:location.href;document.querySelector('#qr .qr-card strong').textContent=link;document.querySelector('#qr .qr-meta').innerHTML=e?`<span>${escapeHtml(e.name)}</span><span>${e.photos} photos</span><span>Local upload</span>`:''}
async function openResults(matches=null){showPage('results');await renderPhotos(matches)}
async function renderPhotos(matches=null){const e=events.find(x=>x.id===currentEventId);const photos=await dbGetPhotos(currentEventId);document.getElementById('resultsSub').textContent=e?`${photos.length} photos stored on this browser.`:'Select an event first.';document.getElementById('photoGrid').innerHTML=photos.length?photos.filter(p=>!matches||matches.has(p.id)).map(p=>`<div class="photo" data-photo-id="${p.id}"><img src="${URL.createObjectURL(p.blob)}" alt="${escapeHtml(p.name)}" loading="lazy"><span>${escapeHtml(p.name)}</span></div>`).join(''):'<p class="muted">No photos uploaded yet.</p>';if(matches&&matches.size===0)document.getElementById('photoGrid').innerHTML='<p class="muted">No matching face found. Try a clear, front-facing selfie.</p>'}
function newEvent(){document.getElementById('eventModal').classList.remove('hidden');document.getElementById('eventName').focus()}
function loadFaceApi(){if(faceApiReady)return faceApiReady;faceApiReady=new Promise((resolve,reject)=>{if(window.faceapi)return resolve();const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/@vladmandic/face-api/dist/face-api.js';s.onload=()=>resolve();s.onerror=()=>reject(new Error('Could not load browser AI library'));document.head.appendChild(s)}).then(async()=>{const base='https://cdn.jsdelivr.net/npm/@vladmandic/face-api/model';await Promise.all([faceapi.nets.ssdMobilenetv1.loadFromUri(base),faceapi.nets.faceLandmark68Net.loadFromUri(base),faceapi.nets.faceRecognitionNet.loadFromUri(base)]);return true});return faceApiReady}
function blobToImage(blob){return new Promise((resolve,reject)=>{const u=URL.createObjectURL(blob);const img=new Image();img.onload=()=>{URL.revokeObjectURL(u);resolve(img)};img.onerror=()=>{URL.revokeObjectURL(u);reject(new Error('Could not read image'))};img.src=u})}
async function descriptorForBlob(blob,multi=false){await loadFaceApi();const img=await blobToImage(blob);const results=multi?await faceapi.detectAllFaces(img,new faceapi.SsdMobilenetv1Options({minConfidence:.45})).withFaceLandmarks().withFaceDescriptors():await faceapi.detectSingleFace(img,new faceapi.SsdMobilenetv1Options({minConfidence:.5})).withFaceLandmarks().withFaceDescriptor();return results||[]}
async function findMatches(queryBlob){const photos=await dbGetPhotos(currentEventId);const q=await descriptorForBlob(queryBlob,false);if(!q){throw new Error('No face detected in selfie. Please use a clear front-facing selfie.')}const matches=new Set();const threshold=.52;for(const p of photos){try{const faces=await descriptorForBlob(p.blob,true);for(const f of faces){if(faceapi.euclideanDistance(q.descriptor,f.descriptor)<=threshold){matches.add(p.id);break}}}catch(e){console.warn('Skipping photo',p.name,e)}}return matches}
function setupCamera(){const modal=document.createElement('div');modal.className='camera-modal';modal.innerHTML=`<div class="camera-card"><button class="close camera-close">×</button><p class="eyebrow">SELFIE SEARCH</p><h2>Take your selfie</h2><div class="camera-frame"><video autoplay playsinline muted></video><div class="face-guide"></div></div><p class="muted camera-status">Loading camera…</p><div class="camera-actions"><button class="secondary-btn camera-close">Cancel</button><button class="primary-btn capture-btn">Capture & find photos</button></div></div>`;document.body.appendChild(modal);const video=modal.querySelector('video');let stream=null;const stop=()=>{stream?.getTracks().forEach(t=>t.stop());currentStream=null;modal.remove()};modal.querySelectorAll('.camera-close').forEach(b=>b.onclick=stop);navigator.mediaDevices?.getUserMedia({video:{facingMode:'user',width:{ideal:720},height:{ideal:720}},audio:false}).then(s=>{stream=s;currentStream=s;video.srcObject=s;modal.querySelector('.camera-status').textContent='Centre your face, then press Capture.'}).catch(()=>{modal.querySelector('.camera-status').textContent='Camera permission failed. You can test by adding a selfie file in the next version.'});modal.querySelector('.capture-btn').onclick=async()=>{if(!video.videoWidth){modal.querySelector('.camera-status').textContent='Camera is not ready yet.';return}const canvas=document.createElement('canvas');canvas.width=video.videoWidth;canvas.height=video.videoHeight;canvas.getContext('2d').drawImage(video,0,0);canvas.toBlob(async blob=>{const button=modal.querySelector('.capture-btn');button.disabled=true;button.textContent='AI searching…';try{const matches=await findMatches(blob);stop();document.getElementById('resultsEyebrow').textContent='AI FACE SEARCH';document.getElementById('resultsTitle').textContent=`Found ${matches.size} matching photo${matches.size===1?'':'s'}`;await openResults(matches)}catch(e){button.disabled=false;button.textContent='Capture & find photos';modal.querySelector('.camera-status').textContent=e.message}},'image/jpeg',.92)};}
function previewLocalFiles(){const input=document.getElementById('localPhotos');const files=[...input.files].filter(f=>/^image\/(jpeg|png|webp)$/.test(f.type));const box=document.getElementById('uploadPreview');box.innerHTML=files.length?`<strong>${files.length} photo${files.length>1?'s':''} selected</strong><small>They will be saved in this browser.</small>`:'<span>No photos selected</span>';if(files.length!==input.files.length)box.innerHTML='<strong>Only JPG, PNG or WEBP images are supported.</strong>'}
async function createLocalEvent(){const button=document.getElementById('createEvent');const name=document.getElementById('eventName').value.trim()||'New Photography Event';const date=document.getElementById('eventDate').value||'Local test';const files=[...document.getElementById('localPhotos').files].filter(f=>/^image\/(jpeg|png|webp)$/.test(f.type));if(!files.length){document.getElementById('uploadPreview').innerHTML='<strong>Please choose at least one JPG, PNG or WEBP photo.</strong>';return}button.disabled=true;button.textContent='Saving photos…';try{const id=`local-${Date.now()}-${eventCounter++}`;await dbPut('events',{id,name,date,photos:files.length,status:'READY'});for(const [i,file] of files.entries()){await dbPut('photos',{id:`${id}-${i}-${crypto.randomUUID()}`,eventId:id,name:file.name,mime:file.type,size:file.size,blob:file,createdAt:Date.now()})}events.unshift({id,name,date,photos:files.length,status:'READY'});currentEventId=id;renderEvents();document.getElementById('eventModal').classList.add('hidden');document.getElementById('eventName').value='';document.getElementById('eventDate').value='';document.getElementById('localPhotos').value='';document.getElementById('uploadPreview').innerHTML='';await openResults();}catch(error){console.error(error);alert(`Save failed: ${error.message}`)}finally{button.disabled=false;button.textContent='Create event & upload'}}
async function boot(){try{const stored=await dbGetAll('events');events.push(...stored.sort((a,b)=>(b.createdAt||0)-(a.createdAt||0)));renderEvents()}catch(e){console.error('Local database unavailable',e);renderEvents()}const eventParam=new URLSearchParams(location.search).get('event');if(eventParam){currentEventId=eventParam;await openGuest(eventParam)}}
document.getElementById('newEventBtn').addEventListener('click',newEvent);document.getElementById('newEventBtn2').addEventListener('click',newEvent);document.getElementById('closeModal').addEventListener('click',()=>document.getElementById('eventModal').classList.add('hidden'));document.getElementById('viewGuestBtn').addEventListener('click',()=>openGuest());document.getElementById('selfieBtn').addEventListener('click',setupCamera);document.getElementById('demoMatchBtn').addEventListener('click',openResults);document.getElementById('backGuestBtn').addEventListener('click',()=>openGuest(currentEventId));document.getElementById('localPhotos').addEventListener('change',previewLocalFiles);document.getElementById('createEvent').addEventListener('click',createLocalEvent);document.getElementById('copyLinkBtn').addEventListener('click',async()=>{const link=document.querySelector('#qr .qr-card strong').textContent;try{await navigator.clipboard.writeText(link);document.getElementById('copyLinkBtn').textContent='Copied ✓';setTimeout(()=>document.getElementById('copyLinkBtn').textContent='Copy guest link',1800)}catch{alert(link)}});document.querySelectorAll('.topnav a').forEach(a=>a.addEventListener('click',e=>{e.preventDefault();showPage(a.getAttribute('href').slice(1))}));document.getElementById('menuBtn').addEventListener('click',()=>document.querySelector('.topnav').classList.toggle('mobile-open'));boot();
